◆ビルド手順

　1.proto-o/ 直下に適当な名前のディレクトリを作成する。
      ex.) mkdir build

  2.作成したディレクトリに移動する。
      ex.) cd build

  3.cmake を実行する。
      ex.) cmake ../

  4.手順1で作成したディレクトリに make ファイルが作成されるので、make を実行する。

  5.「proto-o」という名前の実行ファイルが作成されるので、それを実行する。

